package com.meteoradesigner.model;

//TODO fix all the documentation, by using this class, -es and dots.
/**
 * PointedCompletionState - not the entity, pointed completion state for a task that is in
 * <code>FROZEN</code> or <code>FROZEN_WITH_FORCIBLY_UNLOCKED_RELATIVES_BY</code>.
 */
public enum PointedCompletionState {
    POINTED_COMPLETION_STATE_FROZEN_BY
}
